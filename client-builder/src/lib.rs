// This is a build tool for generating OpenFGA client code.
// The actual client code is generated and copied to the openfga-client crate.
//
// To regenerate the client code, run: cargo build -p client-builder

pub fn build_client() {
    println!(
        "OpenFGA client builder - run `cargo build -p client-builder` to generate client code"
    );
}
